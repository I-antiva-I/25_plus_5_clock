export enum PhaseType
{
    BREAK,
    SESSION,
    BOTH,
}

export enum ClockStatus
{
    ACTIVE,
    PAUSED,
}

export enum ThemeType
{
    DARK,
    LIGHT
}

export enum IconType
{
    PLUS,
    MINUS,

    PAUSE,
    PLAY,
    SKIP,
    RESET,

    MOON,
    SUN,
    INFO,
}

